# Core dependencies
PyQt5>=5.15.0
playwright>=1.40.0
requests>=2.28.0
tldextract>=5.1.0
lxml>=4.9.0
pymongo>=4.6.0
dnspython>=2.4.0
PySocks>=1.7.1

# Optional: for window minimization on Windows
pywin32>=306; sys_platform == 'win32'

# For building executable
pyinstaller>=6.0.0
